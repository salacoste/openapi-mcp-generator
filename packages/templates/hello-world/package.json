{
  "name": "hello-world-mcp",
  "version": "1.0.0",
  "description": "A minimal MCP server generated by openapi-to-mcp",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "hello-world-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "0.5.0",
    "dotenv": "16.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
