{
  "name": "@openapi-to-mcp/templates",
  "version": "0.1.0",
  "description": "MCP server templates for code generation",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "echo 'Templates package contains static files'",
    "test": "echo 'No tests for templates package'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
