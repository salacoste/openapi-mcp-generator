# ⚠️ SECURITY WARNING ⚠️
# This file contains example environment variables.
# NEVER commit your actual .env file to version control!
#
# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Replace placeholder values with your actual credentials
# 3. Keep .env private (it's in .gitignore)
# 4. For production, use secure secret management (AWS Secrets Manager, Vault, etc.)
#
# See SECURITY.md for detailed security best practices

# =============================================================================
# API Configuration
# =============================================================================

# Base URL for API requests
# ⚠️ Always use HTTPS in production!
API_BASE_URL={{primaryServer.url}}

# Request timeout in milliseconds
API_TIMEOUT=30000

# Enable debug logging (set to 'false' in production)
# ⚠️ Debug mode may expose sensitive information in logs
DEBUG=false

# =============================================================================
# Authentication Credentials
# =============================================================================
{{#if hasApiKey}}

{{#each apiKeySchemes}}
# API Key for {{name}} ({{in}} authentication)
# ⚠️ NEVER commit your real API key!
# ⚠️ Rotate this key every 30-90 days
# Get your API key from: {{../apiDocUrl}}
{{#if (eq in 'header')}}
API_KEY=your-api-key-here
{{else if (eq in 'query')}}
API_KEY=your-api-key-here
{{else if (eq in 'cookie')}}
API_KEY=your-api-key-here
{{/if}}
{{/each}}
{{/if}}
{{#if hasBearerToken}}

# Bearer Token Authentication
# ⚠️ NEVER commit your real token!
# ⚠️ Tokens should have expiration times
# ⚠️ Rotate tokens immediately if compromised
BEARER_TOKEN=your-bearer-token-here
{{/if}}
{{#if hasBasicAuth}}

# Basic Authentication Credentials
# ⚠️ NEVER commit real credentials!
# ⚠️ Use strong passwords (12+ characters, mixed case, numbers, symbols)
# ⚠️ Different credentials for dev/staging/production
BASIC_AUTH_USERNAME=your-username
BASIC_AUTH_PASSWORD=your-secure-password
{{/if}}

# =============================================================================
# Security Checklist (Remove after review)
# =============================================================================
#
# Before deploying:
# [ ] All placeholder values replaced with real credentials
# [ ] .env file is NOT committed to git (check with: git status)
# [ ] HTTPS is used for API_BASE_URL (not HTTP)
# [ ] DEBUG is set to 'false' in production
# [ ] Credentials use least privilege (minimal required permissions)
# [ ] Team members have individual credentials (not shared)
# [ ] Credentials are stored in secret manager for production (not .env)
#
# See SECURITY.md for complete security guidelines

# =============================================================================
# Generated by OpenAPI-to-MCP Generator
# https://github.com/openapi-to-mcp
# =============================================================================
